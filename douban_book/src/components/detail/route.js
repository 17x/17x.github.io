import Detail from './index';
import React from 'react';

const detailState = [
    {
        name: 'detail',
        url: '/detail?:id',
        component: Detail
    }
];

export default detailState;