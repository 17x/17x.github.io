import Home from './index';
import React, {Component} from 'react';

const homeState = [
    {
        name: 'home',
        url: '/home',
        // sticky: true,
        // redirectTo: 'home.tab1',
        component: Home
    }
];

export default homeState;